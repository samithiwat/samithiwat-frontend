export type ModalProps = {
	width: number;
	height: number;
	bgColor: string;
	padding: number;
	gap: number;
};

export type ModalImageProps = {
	imgUrl: string;
};

export type ModalStatus = {
	isOpen: boolean;
	carousel: boolean;
};
